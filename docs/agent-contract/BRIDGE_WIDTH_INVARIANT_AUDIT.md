# Bridge width invariant audit

## Rollback baseline

- Branch before the first contract edit: `dev`
- Exact commit before the first contract edit: `6692dc4f4af4bc3b00c86775b5eecb5e2756660d`
- Durable local rollback reference: `rollback-before-bridge-width-invariant-20260907`
- Scope: all bridge types generated by the mod except `TrussArch02`, which was explicitly excluded
  from this pass because it had just been modified.

## Measurement rule

For each bridge type, measure the real archetype and a real newly generated bridge on the same width
basis. The generated result must satisfy:

    generated bridge width - generated deck width
        = archetype bridge width - archetype deck width

The skip threshold is the maximum absolute x-coordinate correction required by the measured output.
When that correction exceeds 1 m, the bridge is recorded and left unchanged.

## Preserved real generated samples

| Bridge type | Generated bridge and road | Evidence | Result |
| --- | --- | --- | --- |
| `TrussArch02` | `两块板六车道_TrussArch02`; measured road surface 40 m and visible road width 42 m | Current `BridgeBuilder/last-export-report.txt`; real generated sample exists | Excluded from this pass as requested |
| `ExtradosedLarge` | `两块板六车道_ExtradosedLarge`; measured deck 40 m | Legacy `BridgePrefabGenerator/last-export-report.txt`; the 61 m donor geometry was asked to lose 21 m, its side coordinate displacement was -10.5 m, and the output collapsed to 0 m | **Skipped**: required absolute coordinate correction is at least 10.5 m, greater than 1 m; separate bridge-specific diagnosis is required |

The reports above are retained game export reports, not synthetic fixtures. The legacy
`ExtradosedLarge` output is evidence of a failure and cannot be used as proof that the invariant is
satisfied.

## Missing real generated samples

No retained newly generated bridge measurement was found for the bridge types below. Source formulas
and archetype-only measurements are intentionally not substituted for the missing comparison.

| Bridge type | Status | Required next evidence |
| --- | --- | --- |
| `Extradosed01` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `Extradosed02` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `Extradosed03` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `CableStayed` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `Suspension` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `SuspensionGolden` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `TrussArch01` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `TrussArch03` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `TrussArch` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `TiedArch` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `CoveredWood` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |
| `Grand` | Unverified; unchanged | Human creates one bridge from any compatible road and retains the export report and geometry dump |

`Draw`, `PedestrianDraw` and `Lift` remain deferred designs under contract section 10 and are not
generated; there is no generated output to correct in this pass.

## Human sample boundary

The user has required game operation to remain a human visual-judgment step. Consequently this audit
does not launch the game or silently add an automatic export path. For each unverified type, the next
step is to create a bridge manually from any compatible road, then preserve
`ModsData/BridgeBuilder/last-export-report.txt` and the corresponding archetype/generated geometry
dumps before generating the next sample. Only then may the same-basis constant and correction be
calculated and the `1 m` gate applied.
